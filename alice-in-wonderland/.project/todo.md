# Alice in Wonderland — TODO

## Reader Formats

| File | Status | Notes |
|------|--------|-------|
| reader.html | Active | 5 themes, half-sun toggle, sidebar, progress bar, `#ch-N` hash |
| fullbleed.html | Active | 5 themes, page flip, running headers, chapter scrubber, `#p-N` hash |
| index.html | Legacy | Two-page book spread (not a landing page), 5 themes |
| mobile.html | Legacy | Mobile-first swipe reader — generated by `gen_mobile.py`, 5 themes |
| scroll.html | Legacy | Simple continuous scroll, 5 themes, j/k/arrow/space keys |
| single.html | Legacy | Paginated single page, 5 themes |
| web-reader.html | Legacy | Browser-style reader (own light/sepia/dark/black set) |
| pdf-reader.html | Legacy | PDF viewer simulation, 5 themes |
| theater.html | Legacy | Immersive reading, 5 themes, seekable progress bar, `#s-N` hash |

## Pending

- [ ] Add seeds/chapters.json (content currently embedded in each HTML file)
- [ ] Consider consolidating legacy readers or archiving unused ones (user's call — do not delete)
- [ ] Mobile testing for fullbleed.html
- [ ] Bring `web-reader.html` onto the five house theme names (currently light/sepia/dark/black)
- [ ] `mobile.html` theme button is a 17×20px touch target — needs the ::before hit expander
- [ ] `reader.html` `#ch-N` restore is racy: `restoreHash()` fires on `document.fonts.ready + 50ms`,
      and when the font request is slow the scroll lands before layout settles, so the page ends up
      at chapter 1. Reproduces ~50% of loads (pre-existing; same behaviour before this session's
      changes). Needs a post-layout re-check, not a longer timeout.

## Completed

- [x] URL hash state in reader.html (`#ch-N`)
- [x] URL hash state in fullbleed.html (`#p-N` / `#s-N`)
- [x] 5th theme (sepia) — all readers except web-reader.html now carry the house five
- [x] Book-prefixed prefs keys everywhere (`alice-{format}-prefs`, legacy keys read as fallback)
- [x] URL hash position + seekable progress bar in theater.html (`#s-N`)
- [x] Keyboard nav (j/k/arrows/space) in scroll.html; chapter-row edge fades
- [x] aria-labels on every icon-only control; theme dots are real `<button>`s with 40px hit areas
- [x] Small-screen fixes: single.html footer, index.html controls bar, pdf-reader.html toolbar
- [x] 4-theme system in reader.html and fullbleed.html
- [x] Half-sun theme toggle icon
- [x] Running headers in fullbleed.html (per-page, inside content)
- [x] Recto chapter starts in fullbleed.html
- [x] Chapter scrubber with roman numerals
- [x] Editable page number
- [x] Cover page matching theme
- [x] Theme picker on cover
- [x] TOC on recto (right page)
- [x] Accent color threading through decorative elements
- [x] .project/ directory
- [x] CLAUDE.md build documentation (incl. gen_mobile.py and its /tmp/alice_chapters.json source)
